---
repos:
  - repo: local
    hooks:
      - id: markdownlint
        name: Markdown Lint
        entry: markdownlint
        language: system
        types: [markdown]
        args: [
          "--config=.markdownlint.json",
        ]